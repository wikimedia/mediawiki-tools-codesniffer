<?php

$var = (bool)true;
$var = !false;
$var = true && !false;
$var = (bool)$var ?: !true || !false;

if ( (bool)$var === (bool)$var ) {
	return;
}

$var = (bool)true;
$var = (bool)$var;
$var = (bool)!$var;
$var = (int)(bool)$var;
$var = !!$var instanceof \Title;

if ( !!$var instanceof \Title ) {
	return;
}

if ( !!$var
	instanceof \Title
) {
	return;
}

if ( !!$var ||
	$var instanceof \Title
) {
	return;
}
