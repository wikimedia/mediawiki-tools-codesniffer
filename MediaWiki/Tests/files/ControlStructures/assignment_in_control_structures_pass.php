<?php

if ( $foo == 1 ) {
	# code...
} elseif ( $foo === 1 ) {
	# code...
} elseif ( $foo === [ 1 => 0 ] ) {
	# code...
} elseif ( $foo > 1 || $bar < 1 ) {
	# code...
} elseif ( $foo <= 1 ) {
	# code..
}

while ( $foo !== 1 ) {
	# code...
}
