<?php

if ( $a or $b ) {
	$c = $a and $b;
}

$foo = `echo "hi";`;

class Foo {

	/**
	 * Do stuff
	 */
	public static function doStuff() {
		self::doStuff();
	}
}

$s = $sum / ( ( 1 << 16 ) - 1 ) + 2 * -1 * 3 - 4 ^ 3 % 10;
